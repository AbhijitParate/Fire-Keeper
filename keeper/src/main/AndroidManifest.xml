<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.abhijitpparate.keeper">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:glEsVersion="0x00020000" android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_keeper"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_keeper"
        android:supportsRtl="true">
        <activity
            android:name="com.github.abhijitpparate.keeper.screen.login.LoginActivity"
            android:theme="@style/KeepTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.github.abhijitpparate.keeper.screen.home.HomeActivity"
            android:theme="@style/KeepTheme.NoActionBar"/>

        <activity
            android:parentActivityName="com.github.abhijitpparate.keeper.screen.home.HomeActivity"
            android:name="com.github.abhijitpparate.keeper.screen.note.NoteActivity"
            android:windowSoftInputMode="adjustPan"
            android:theme="@style/KeepTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.github.abhijitpparate.keeper.screen.home.HomeActivity" />
        </activity>

        <meta-data android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

        <activity android:name="com.facebook.FacebookActivity"
            android:configChanges=
                "keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBuqbcueGalSJFJ3mNJGVszVgHgBNF6Oi4"/>

    </application>

</manifest>